#### 一、 项目引言

本项目旨在开发一套基于深度学习的日语口语辅助系统。系统通过构建多任务学习神经网络，解决日语学习中“发音纠错”与“语音听写”两大核心痛点。项目不仅包含后端核心算法模型的设计与训练，还包含完整的前端交互与用户管理系统，是一个典型的全栈AI应用。

#### 二、 系统总体设计

系统采用前后端分离架构，后端提供RESTful API接口，前端负责用户交互，核心算法模型独立部署。

**技术栈选型**

- **前端交互层**：Vue.js 3 + Element Plus（或 React + Ant Design）。负责音频录制、文件上传、结果可视化（汉字注音高亮）。
- **后端服务层**：Python + FastAPI。负责业务逻辑处理、用户鉴权、音频预处理、模型调度。
- **算法模型层**：PyTorch。
    - 骨干网络：Conformer 或 CNN+BiLSTM（提取声学特征）。
    - 任务头A（评估）：基于注意力机制的音素级打分网络。
    - 任务头B（识别）：CTC/Attention Decoder（序列生成）。
- **数据处理层**：MeCab/fugashi（日语分词与G2P转换）、Librosa/Torchaudio（音频特征提取）。
- **数据存储层**：MySQL（用户信息与测试记录）、本地文件系统/OSS（音频文件存储）。

#### 三、 功能需求详解

本系统主要包含三大核心模块：用户中心、发音评估模块、语音识别模块。

**用户中心模块**

- **用户注册与登录**：支持用户名/密码注册，JWT Token鉴权。
- **历史记录查询**：用户可查看所有历史测试记录，包括测试时间、测试类型（评估/识别）、参考文本、识别结果及得分详情。

**发音评估模块**

- **文本展示与注音**：系统随机或按序展示日语测试文本（假名、单词、含促音/拗音的句子）。前端需利用 `<ruby>` 标签实现汉字上方的假名注音。
- **音频采集**：
    - 在线录音：调用浏览器麦克风API录制WAV格式音频。
    - 文件上传：支持上传本地MP3/WAV文件。
- **智能评估**：后端模型将用户音频与参考文本进行强制对齐，输出音素级别的准确度得分。
- **结果反馈**：前端以颜色区分发音质量（如：绿色-准确，黄色-一般，红色-错误），并给出总评分。

**语音识别模块**

- **自由输入**：用户无需参考文本，直接进行录音或上传音频。
- **内容识别**：模型将音频转换为日语文本序列。
- **结果展示**：在文本框中显示识别出的假名或汉字，支持用户复制结果。

#### 四、 核心算法与非功能需求

**算法模型指标**

- **识别准确率**：在JSUT测试集上，假名识别准确率（Character Error Rate, CER）需低于 12%。
- **评估相关性**：发音打分需与人工评分具有较高的皮尔逊相关系数。
- **特殊音处理**：模型需重点优化促音（如 `っ`）、拗音（如 `きゃ`）、长音的识别与评估能力。

**非功能需求**

- **响应时间**：音频处理与推理总耗时控制在 3秒以内（不含网络传输）。
- **并发支持**：开发阶段支持单用户调试，架构需预留多用户并发接口。
- **兼容性**：前端需适配主流浏览器（Chrome, Edge, Firefox）。

#### 五、 数据库设计

主要包含两张核心表。

**用户表**

| 字段名 | 类型 | 说明 |
| ------ |------ |------ |
| user_id | INT (PK) | 用户唯一标识 |
| username | VARCHAR | 用户名 |
| password_hash | VARCHAR | 加密后的密码 |
| created_at | DATETIME | 注册时间 |

**测试记录表**

| 字段名 | 类型 | 说明 |
| ------ |------ |------ |
| record_id | INT (PK) | 记录唯一标识 |
| user_id | INT (FK) | 关联用户 |
| test_type | ENUM | 类型：EVALUATE / RECOGNIZE |
| audio_path | VARCHAR | 音频文件存储路径 |
| ref_text | TEXT | 参考文本（评估模式用） |
| result_text | TEXT | 识别结果或评分详情JSON |
| score | FLOAT | 综合得分 |
| created_at | DATETIME | 测试时间 |

#### 六、 接口定义

后端需提供以下核心接口供前端调用。

**用户注册**

- `POST /api/auth/register`
- Body: `{ "username": "string", "password": "string" }`

**用户登录**

- `POST /api/auth/login`
- Return: `{ "token": "string" }`

**发音评估**

- `POST /api/evaluate`
- Header: `Authorization: Bearer <token>`
- Form-Data: `audio` (file), `ref_text` (string)
- Return: `{ "total_score": 85.5, "details": [ { "phoneme": "k", "score": 90 }, ... ] }`

**语音识别**

- `POST /api/recognize`
- Header: `Authorization: Bearer <token>`
- Form-Data: `audio` (file)
- Return: `{ "recognized_text": "こんにちは" }`

**获取历史记录**

- `GET /api/records`
- Return: List of record objects.

#### 七、 开发计划与里程碑

**第一阶段：数据准备与算法原型**

- 下载 JSUT 语料库。
- 编写 Python 脚本，实现音频转梅尔频谱、文本转音素（MeCab）。
- 搭建 PyTorch 模型骨架（Shared Encoder + Dual Heads），跑通训练流程。

**第二阶段：后端开发**

- 搭建 FastAPI 框架。
- 实现用户注册/登录接口与数据库连接。
- 封装模型推理接口，实现音频预处理与结果返回。

**第三阶段：前端开发与联调**

- 搭建 Vue/React 页面，实现录音与上传功能。
- 对接后端接口，实现汉字注音显示与评分可视化。
- 完成历史记录页面的开发。

**第四阶段：测试与论文撰写**

- 系统整体测试，修复 Bug。
- 整理实验数据，撰写毕业设计论文。

#### 八、 风险评估与应对

- **促音/拗音识别困难**：日语的特殊音在声学上特征较短。
    - *应对*：在数据预处理阶段增加特殊音的权重，或在 Loss 函数中增加针对特殊音的惩罚项。
- **前端录音兼容性**：不同浏览器录音API存在差异。
    - *应对*：使用成熟的第三方库如 `Recorder.js` 或 `MediaRecorder API` 的 Polyfill。



